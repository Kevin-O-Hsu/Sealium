# 开发/测试锁文件输入（MEDIUM-004）：在服务端运行时锁文件之上叠加测试工具。
#
# 注意：不含 `-e .`——可编辑安装与 --generate-hashes 不兼容（pip 无法对本地源码
# 项目计算单一 hash）。CI 先 `pip install -r requirements-dev.txt` 装锁定依赖，
# 再 `pip install --no-deps -e .` 装项目本体（依赖已由锁文件提供，--no-deps 跳过解析）。
#
# 生成锁定版：pip-compile --generate-hashes --resolver=backtracking requirements-dev.in -o requirements-dev.txt
-r requirements.in
pytest>=8
httpx
pip-audit>=2.7
pip-tools>=7
